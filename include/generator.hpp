#pragma once

#include "myLib.hpp"
#include <random>

#define ASCII_CHAR_MIN 33
#define ASCII_CHAR_MAX 123

int generateIntInRange(int, int);
std::string generateRandomWord(int);
std::stringstream generateStream();

