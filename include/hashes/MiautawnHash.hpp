#ifndef DJHASH_HPP
#define DJHASH_HPP

#include "../myLib.hpp"

namespace MiautawnHash {
    string my_hash(string input); //hash generator funciton
}
string int_to_hex(int input); //function that converts integer to hexadecimal string

#endif